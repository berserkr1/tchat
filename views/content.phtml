<?php
	if (isset($_GET['page']) ) {
		if ($_GET['page'] == "accueil") {
		require('apps/accueil.php');
		}
		if ($_GET['page'] == "login") {
		require('apps/login.php');
		}
		if ($_GET['page'] == "logout") {
		require('apps/logout.php');
		}
		if ($_GET['page'] == "register") {
			require('apps/register.php');
		}
	}
	else {
		require('apps/accueil.php');
	}
?>